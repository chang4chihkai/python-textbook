# 第 2 章｜變數與資料型態

## 學習目標

- 理解變數的概念與命名規則
- 掌握 Python 的基本資料型態：數字、字串、布林值
- 學會使用各種運算子進行運算
- 了解資料型態之間的轉換方法

---

## 2.1 變數的概念

> **💡 Tip（小技巧）**：在開始寫程式之前，先用演算法的形式規劃你的程式（或其底層問題），這是一個很好的習慣。

### 什麼是變數？

**變數（Variable）** 是用來儲存資料的「容器」。就像貼上標籤的盒子，變數名稱是標籤，變數值是盒子裡的東西。

```
┌─────────────┐
│   變數      │
│  ┌───────┐  │
│  │  值   │  │
│  └───────┘  │
│   name      │  ← 變數名稱
└─────────────┘
```

### 建立變數

在 Python 中，建立變數非常簡單：

```python
# 建立變數
name = "小華"      # 文字（字串）
age = 20           # 整數
height = 175.5     # 小數（浮點數）
is_student = True  # 布林值

# 輸出變數的值
print(name)
print(age)
print(height)
print(is_student)
```

### 變數命名規則

| 規則 | 正確範例 | 錯誤範例 |
|------|----------|----------|
| 只能使用英文、數字、底線 | `my_name`, `age2` | `my-name`, `2age` |
| 不能以數字開頭 | `score1` | `1score` |
| 大小寫有區分 | `Name` 和 `name` 是不同變數 | - |
| 不能使用 Python 保留字 | `if`, `for`, `print` | `if = 5` |
| 建議使用有意義的名稱 | `student_name` | `x`, `abc` |

### 好的命名習慣

```python
# ✅ 好：名稱有意義
student_name = "小明"
total_score = 100
is_passed = True

# ❌ 差：名稱無意義
a = "小明"
x = 100
```

> **📝 Note（說明）**：早期章節的大多數程式都遵循三個步驟：輸入（Input）、處理（Process）和輸出（Output），稱為 **IPO**。輸入用於接收使用者資料，處理用於產生結果，輸出用於顯示結果。

---

## 2.2 基本資料型態

Python 中有多種資料型態，以下介紹最基礎的三種：

### 2.2.1 數值型態

#### 整數（int）

整數是不含小數點的數字：

```python
# 整數運算
a = 10
b = 3

print(a + b)    # 加法：13
print(a - b)    # 減法：7
print(a * b)    # 乘法：30
print(a / b)    # 除法：3.333...
print(a // b)   # 整數除法：3（取整數部分）
print(a % b)    # 取餘數：1
print(a ** b)   # 次方：10^3 = 1000
```

#### 浮點數（float）

浮點數是帶小數點的數字：

```python
# 浮點數
price = 99.9
pi = 3.14159
negative = -5.5

# 科學記號
large_number = 1.5e6   # 1.5 × 10^6 = 1500000
small_number = 2e-3    # 2 × 10^-3 = 0.002
```

### 2.2.2 字串（str）

**字串（String）** 是用來表示文字的資料型態，用引號包住：

```python
# 建立字串
name = "小明"
message = '你好'

# 多行字串（三引號）
poem = """
床前明月光，
疑是地上霜，
舉頭望明月，
低頭思故鄉。
"""

# 字串運算
first = "Hello"
second = "World"

print(first + second)      # 字串連接：HelloWorld
print(first + " " + second)  # 加入空格：Hello World
print(first * 3)           # 重複輸出：HelloHelloHello
```

### 2.2.3 布林值（bool）

**布林值（Boolean）** 只有兩種：`True`（真）和 `False`（假）：

```python
# 布林值
is_adult = True
has_license = False

# 比較運算的結果也是布林值
print(10 > 5)    # True
print(3 == 3)    # True（兩個等號是「等於」）
print(5 != 3)    # True（「不等於」）
print(10 < 5)    # False
```

---

## 2.3 運算子

### 算術運算子

| 運算子 | 範例 | 說明 |
|--------|------|------|
| + | a + b | 加法 |
| - | a - b | 減法 |
| * | a * b | 乘法 |
| / | a / b | 除法 |
| // | a // b | 整數除法 |
| % | a % b | 取餘數 |
| ** | a ** b | 次方 |

### 比較運算子

| 運算子 | 範例 | 說明 |
|--------|------|------|
| == | a == b | 等於 |
| != | a != b | 不等於 |
| > | a > b | 大於 |
| < | a < b | 小於 |
| >= | a >= b | 大於等於 |
| <= | a <= b | 小於等於 |

### 邏輯運算子

| 運算子 | 範例 | 說明 |
|--------|------|------|
| and | a and b | 兩者都為 True 才 True |
| or | a or b | 任一者為 True 即 True |
| not | not a | 反轉 True/False |

```python
# 邏輯運算範例
age = 25

print(age >= 18 and age < 65)   # True：成年且未退休
print(age < 18 or age >= 65)    # False：不是未成年就是退休
print(not age >= 18)           # False：不是成年
```

---

## 2.4 型態轉換

有時需要將資料從一種型態轉換成另一種：

### 常見的型態轉換

```python
# 轉換為整數
int(3.7)       # 3（小數點後直接捨去）
int("10")      # 10
int(True)      # 1

# 轉換為浮點數
float(5)       # 5.0
float("3.14")  # 3.14

# 轉換為字串
str(123)       # "123"
str(3.14)      # "3.14"

# 轉換為布林值
bool(1)        # True
bool(0)        # False
bool("")       # False（空字串）
bool("abc")    # True
```

### 實用範例：計算BMI

```python
# 計算 BMI（身體質量指數）
name = "小美"
height = 1.65    # 身高（公尺）
weight = 55      # 體重（公斤）

# BMI = 體重 / 身高²
bmi = weight / (height ** 2)

print(name, "的 BMI 是", bmi)
print("BMI 為", round(bmi, 1))  # round() 四捨五入到小數點後1位
```

執行結果：
```
小美 的 BMI 是 20.202020202020202
BMI 為 20.2
```

---

## 2.5 input() 互動輸入

`input()` 函式可以讓使用者輸入資料：

```python
# 基本用法
name = input("請輸入你的名字：")
print("你好，" + name)

# 輸入數字需要轉型
age = int(input("請輸入你的年齡："))
print("你明年", age + 1, "歲")

# 練習：計算兩個數的和
num1 = float(input("第一個數："))
num2 = float(input("第二個數："))
print("總和是：", num1 + num2)
```

---

## 2.6 Turtle 繪圖：使用變數控制畫圖

學會了變數，我們就可以用變數來控制烏龜畫圖！這樣我們可以輕鬆地修改數值來改變圖案。

### 用變數控制距離和角度

```python
import turtle

# 使用變數來控制
length = 100      # 邊長
angle = 90       # 角度

# 畫正方形
turtle.forward(length)
turtle.right(angle)
turtle.forward(length)
turtle.right(angle)
turtle.forward(length)
turtle.right(angle)
turtle.forward(length)

turtle.done()
```

### 用變數畫出不同大小的圖案

```python
import turtle

# 設定圓的半徑
radius = 80

# 畫一個半徑 80 的圓
turtle.circle(radius)

# 把半徑改小，再畫一個
radius = 50
turtle.penup()
turtle.goto(150, 0)
turtle.pendown()
turtle.circle(radius)

turtle.done()
```

### 練習：畫出你自己的圖案

```python
import turtle

# 自行設定變數
side_length = 80   # 邊長
num_sides = 6      # 邊數（嘗試改成 3, 4, 5, 6...）
angle = 360 / num_sides  # 自動計算角度

# 畫多邊形
for i in range(num_sides):
    turtle.forward(side_length)
    turtle.right(angle)

turtle.done()
```

### 小挑戰

使用變數畫出：
1. 三個不同大小的圓
2. 一個正方形和一個長方形
3. 使用者輸入邊數和邊長的多邊形

---

## 2.7 實用範例 - 小遊戲

### 遊戲 1：戰鬥計算機

這是一個角色扮演遊戲中的戰鬥傷害計算機！

```python
# 戰鬥計算機
# 學習重點：變數、運算子、input()、型態轉換

print("=" * 50)
print("        ⚔️ 戰鬥傷害計算機 ⚔️")
print("=" * 50)
print()

# 玩家基本資料
player_name = input("請輸入勇者名稱：")
player_level = int(input("請輸入勇者等級 (1-100)："))
player_attack = int(input("請輸入勇者攻擊力 (1-100)："))

print()
print("-" * 50)
print("敵人出現了！")
print()

# 敵人資料
enemy_name = "哥布林王"
enemy_level = int(input(f"請輸入 {enemy_name} 的等級："))
enemy_hp = int(input(f"請輸入 {enemy_name} 的血量："))
enemy_defense = int(input(f"請輸入 {enemy_name} 的防禦力："))

print()
print("=" * 50)
print("         ⚔️ 戰鬥開始！ ⚔️")
print("=" * 50)
print()

# 傷害計算公式
# 傷害 = 攻擊力 × (1 + 等級/100) - 防禦力 × 0.5
# 最小傷害為 1
base_damage = player_attack * (1 + player_level / 100)
damage_reduction = enemy_defense * 0.5
final_damage = int(base_damage - damage_reduction)

# 確保傷害至少為 1
if final_damage < 1:
    final_damage = 1

# 計算攻擊次數
import math
attack_count = math.ceil(enemy_hp / final_damage)

# 顯示戰鬥結果
print(f"{player_name} 對 {enemy_name} 發動攻擊！")
print(f"基礎傷害：{player_attack}")
print(f"等級加成：+{int(player_attack * player_level / 100)}")
print(f"敵人防禦：-{int(damage_reduction)}")
print()
print(f"💥 最終傷害：{final_damage}！")
print()
print(f"敵人血量：{enemy_hp}")
print(f"需要攻擊次數：{attack_count} 次")
print()

# 剩餘血量
remaining_hp = enemy_hp - final_damage
if remaining_hp < 0:
    remaining_hp = 0

print(f"敵人剩餘血量：{remaining_hp}")
print()

if remaining_hp == 0:
    print("🎉 敵人被打倒了！")
    print("你是真正的勇者！")
else:
    print("⚠️ 敵人還沒倒下！")
    print("繼續攻擊！")

print()
print("=" * 50)
print("         戰鬥結束！")
print("=" * 50)
```

執行範例：
```
==================================================
        ⚔️ 戰鬥傷害計算機 ⚔️
==================================================

請輸入勇者名稱：小明
請輸入勇者等級 (1-100)：10
請輸入勇者攻擊力 (1-100)：50

--------------------------------------------------
敵人出現了！

請輸入 哥布林王 的等級：8
請輸入 哥布林王 的血量：200
請輸入 哥布林王 的防禦力：15

==================================================
         ⚔️ 戰鬥開始！ ⚔️
==================================================

小明 對 哥布林王 發動攻擊！
基礎傷害：50
等級加成：+5
敵人防禦：-7

💥 最終傷害：48！

敵人血量：200
需要攻擊次數：5 次

敵人剩餘血量：152

⚠️ 敵人還沒倒下！
繼續攻擊！

==================================================
         戰鬥結束！
==================================================
```

---

### 遊戲 2：數學大冒險

這是一個結合數學問題的冒險遊戲，答對問題就能打敗怪物！

```python
# 數學大冒險
# 學習重點：變數、運算子、input()、型態轉換、f-string

import random
import time

print("=" * 50)
print("        📚 數學大冒險 📚")
print("=" * 50)
print()
print("勇者，你必須回答數學問題來打敗怪物！")
print("每答对一題，就能給怪物造成傷害！")
print()

# 玩家設定
player_name = input("請輸入勇者名稱：")
player_hp = 100  # 玩家血量

# 怪物設定
monster_name = "數學惡龍"
monster_hp = 500  # 怪物血量

print()
print("🐉 出現了巨大的數學惡龍！")
print(f"{monster_name} 的血量：{monster_hp}")
print()

# 戰鬥回合
round_number = 1
while monster_hp > 0 and player_hp > 0:
    print("-" * 50)
    print(f"第 {round_number} 回合")
    print("-" * 50)
    
    # 產生隨機數學問題
    operator = random.choice(["+", "-", "*"])
    
    if operator == "+":
        a = random.randint(1, 50)
        b = random.randint(1, 50)
        correct_answer = a + b
        question = f"{a} + {b} = ?"
        
    elif operator == "-":
        a = random.randint(50, 100)
        b = random.randint(1, a)  # 確保結果為正數
        correct_answer = a - b
        question = f"{a} - {b} = ?"
        
    else:
        a = random.randint(2, 12)
        b = random.randint(2, 10)
        correct_answer = a * b
        question = f"{a} × {b} = ?"
    
    # 顯示問題
    print(f"問題：{question}")
    
    # 計時
    start_time = time.time()
    player_answer = int(input("請輸入答案："))
    end_time = time.time()
    time_used = end_time - start_time
    
    # 檢查答案
    if player_answer == correct_answer:
        # 計算傷害和得分
        base_damage = 50
        time_bonus = max(0, int(20 - time_used * 2))  # 越快分數越高
        total_damage = base_damage + time_bonus
        
        monster_hp = monster_hp - total_damage
        
        print()
        print(f"✅ 正確！")
        print(f"   攻擊造成 {total_damage} 傷害！")
        if time_bonus > 0:
            print(f"   速度 bonus：+{time_bonus}")
        
    else:
        # 答錯了，怪物反擊
        print()
        print(f"❌ 錯誤！正確答案是 {correct_answer}")
        
        # 怪物反擊傷害
        monster_damage = random.randint(10, 25)
        player_hp = player_hp - monster_damage
        
        print(f"   怪物反擊！受到 {monster_damage} 傷害！")
    
    # 顯示雙方狀態
    print()
    print(f"【狀態】")
    print(f"  勇者 {player_name}: HP {player_hp}")
    if monster_hp > 0:
        print(f"  {monster_name}: HP {monster_hp}")
    else:
        print(f"  {monster_name}: HP 0")
    
    round_number = round_number + 1
    
    # 檢查遊戲結束
    if monster_hp <= 0:
        print()
        print("=" * 50)
        print("🎉 恭喜你打敗了數學惡龍！")
        print("你是數學天才！")
        print("=" * 50)
        break
    
    if player_hp <= 0:
        print()
        print("=" * 50)
        print("😵 你被怪物打敗了...")
        print("不要氣餒，再練習數學吧！")
        print("=" * 50)
        break

print()
print(f"總共進行了 {round_number - 1} 回合")
print("感謝遊玩數學大冒險！")
```

---

## 2.7 本章小結

| 概念 | 重點 |
|------|------|
| 變數 | 用來儲存資料的容器，用等號 `=` 賦值 |
| 整數 int | 不含小數的數字 |
| 浮點數 float | 含小數的數字 |
| 字串 str | 用引號包住的文字 |
| 布林值 bool | True 或 False |
| 型態轉換 | int()、float()、str()、bool() |
| input() | 讓使用者輸入資料 |

---

## 練習題

### 基礎題

1. **變數練習**：建立三個變數儲存你的名字、學號和系級，並輸出。
2. **計算面積**：輸入長和寬，計算矩形面積。
3. **型態判斷**：使用 `type()` 函式查看以下資料的型態：`10`、`"10"`、`10.0`、`True`。
4. **基本運算**：建立兩個變數 a=15、b=4，輸出它們的和、差、積、商和餘數。
5. **字串變數**：建立變數儲存你的最喜歡的運動，並輸出「我最喜歡的運動是 XXX」。
6. **布林值練習**：建立兩個布林變數 `is_student` 和 `is_adult`，並用 `and`、`or`、`not` 組合它們。
7. **浮點數運算**：計算圓的面積，半徑為 5（使用 3.14）。
8. **資料型態混合**：建立整數、浮點數、字串和布林各一個變數並輸出。
9. **使用者互動**：使用 `input()` 輸入你的名字並輸出「你好，XXX」。
10. **型態轉換練習**：將字串 "100" 轉換為整數並加上 50 後輸出。

### 進階題

1. **單位轉換**：將華氏溫度轉換為攝氏。公式：`C = (F - 32) × 5/9`
2. **計算平均值**：輸入三個成績，計算平均值並顯示。
3. **交換變數**：有兩個變數 `a = 10`、`b = 20`，交換它們的值（不使用第三個變數）。
4. **個人理財**：輸入本月收入和支出，計算餘額並判斷是否為正值。
5. **時鐘計算**：輸入秒數（例如 3665 秒），轉換為「X 小時 Y 分 Z 秒」格式。
6. **BMI 計算**：輸入身高（公分）和體重（公斤），計算 BMI 並顯示。
7. **三角形面積**：使用海龍公式計算三角形面積，輸入三邊長度。
8. **字串格式化**：使用 f-string 輸出「我是 XXX，今年 Y 歲，就讀 Z 系」。

### 挑戰題

1. **購物計算**：輸入商品單價和數量，計算總價並加上 5% 稅金。
2. **複利計算**：輸入本金、年利率和年數，計算複利後的總金額。
3. **時間加法**：輸入兩個時間（時、分），計算它們的和並顯示。

---

## 進一步閱讀

### 更多資料型態

Python 除了本章介紹的基本資料型態外，還有其他常用的型態：

| 型態 | 說明 | 範例 |
|------|------|------|
| `int` | 整數 | `10`, `-5`, `0` |
| `float` | 浮點數 | `3.14`, `-0.5` |
| `str` | 字串 | `"Hello"`, `'Python'` |
| `bool` | 布林值 | `True`, `False` |
| `bytes` | 位元組 | `b"hello"` |
| `None` | 空值 | `None` |

### 更多內建函式

```python
# 型態轉換函式
int()      # 轉為整數
float()    # 轉為浮點數
str()      # 轉為字串
bool()     # 轉為布林值
complex()  # 轉為複數

# 數學相關函式
abs(-5)         # 絕對值 → 5
divmod(10, 3)   # 商和餘數 → (3, 1)
pow(2, 3)       # 次方 → 8
round(3.7)      # 四捨五入 → 4
```

### 運算子優先順序

當表達式包含多個運算子時，Python 會依照優先順序進行計算：

| 優先順序 | 運算子 | 說明 |
|----------|--------|------|
| 1 | `**` | 指數 |
| 2 | `+x`, `-x` | 正負號 |
| 3 | `*`, `/`, `//`, `%` | 乘除餘數 |
| 4 | `+`, `-` | 加減 |

### Python 官方文件

- [內建型態](https://docs.python.org/3/library/stdtypes.html) - 標準資料型態完整說明
- [內建函式](https://docs.python.org/3/library/functions.html) - 所有內建函式參考
- [運算子](https://docs.python.org/3/library/operator.html) - 運算子模組

---

*下一章節，我們將學習條件判斷，讓程式能夠做選擇。*
